# Don't change this
theme: jekyll-theme-primer

# AMWA ID
amwa_id: IS-06

# GitHub project
baseurl: /nmos-network-control

# Which relase or branch appears in header links and on the home page
default_tree: branches/docs-refactor # alternatively e.g. branches/v1.0.x

# Regex patterns of tags (releases) and branches to show
show_tags: 
show_branches: docs-refactor
